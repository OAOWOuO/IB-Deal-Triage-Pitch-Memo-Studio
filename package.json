{
  "name": "ib-deal-triage-pitch-memo-studio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Live public-company investment banking deal triage and pitch memo studio.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check app.js && node --check scripts/smoke.mjs",
    "harness": "node scripts/smoke.mjs",
    "smoke": "node scripts/smoke.mjs",
    "test": "npm run check && npm run harness"
  }
}
